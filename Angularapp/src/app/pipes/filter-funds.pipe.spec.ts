import { FilterFundsPipe } from './filter-funds.pipe';

describe('FilterFundsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFundsPipe();
    expect(pipe).toBeTruthy();
  });
});

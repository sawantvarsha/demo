import { CustomersearchclientsummaryPipe } from './customersearchclientsummary.pipe';

describe('CustomersearchclientsummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomersearchclientsummaryPipe();
    expect(pipe).toBeTruthy();
  });
});

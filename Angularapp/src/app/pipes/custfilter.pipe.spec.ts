import { CustfilterPipe } from './custfilter.pipe';

describe('CustfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustfilterPipe();
    expect(pipe).toBeTruthy();
  });
});

import { DropdownTypeFilterPipe } from './dropdown-type-filter.pipe';

describe('DropdownTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

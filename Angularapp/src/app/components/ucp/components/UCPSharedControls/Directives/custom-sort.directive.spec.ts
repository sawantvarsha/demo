import { CustomSortDirective } from './custom-sort.directive';

describe('CustomSortDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomSortDirective();
    expect(directive).toBeTruthy();
  });
});

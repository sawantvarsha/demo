import { SearchUserGroupPipe } from './search-user-group.pipe';

describe('SearchUserGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchUserGroupPipe();
    expect(pipe).toBeTruthy();
  });
});

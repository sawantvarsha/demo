import { SearchunderlyingPipe } from './searchunderlying.pipe';

describe('SearchunderlyingPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchunderlyingPipe();
    expect(pipe).toBeTruthy();
  });
});

import { EventSearchPipePipe } from './event-search-pipe.pipe';

describe('EventSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EventSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
